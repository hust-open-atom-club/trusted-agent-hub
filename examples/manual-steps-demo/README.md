# demo-manual-steps

演示 `manual_steps` 安装方式的示例包。CLI 只把人工步骤写入
`~/.trusted-agent-hub/installed/demo-manual-steps-manual/steps.txt`，
不执行任何命令，也不需要 `--yes` 确认。

## 安装

```bash
tah install demo-manual-steps --client claude-code
```
